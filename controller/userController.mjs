import users from "../userData/users.json" assert { type: "json" };

import menu from "../userData/menu.json" assert { type: "json" };

import order from "../userData/order.json" assert { type: "json" };

import fs from "fs"

import path, {dirname} from "path";

import { fileURLToPath } from "url";

const __dirname = dirname(fileURLToPath(import.meta.url))

const userController = {


}

export default userController;